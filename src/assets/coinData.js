import {BitcoinData} from './bitcoinData'
import {EthereumData} from './etherumData'
import {LitecoinData} from './litecoinData'

export const CoinData = {
  bitcoin: BitcoinData,
  ethereum: EthereumData,
  litecoin: LitecoinData
}
